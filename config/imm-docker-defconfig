# ImmortalWrt (IMM) rootfs for Docker on RK3566 / 网心云 OEC
#
# Target: armsr/armv8  -- generic ARMv8 (aarch64) SystemReady platform.
#   * 容器与 OEC 宿主共用内核，因此不需要板级 dtb / u-boot / WiFi 驱动。
#   * armsr/armv8 与 RK3566 同为 aarch64，指令集完全兼容。
#
# Output: immortalwrt-armsr-armv8-generic-rootfs.tar.gz  (docker import 即可用)
#
# 用法: cp config/imm-docker-defconfig .config && make defconfig && make -j$(nproc)

# ---------------------------------------------------------------- target
CONFIG_TARGET_armsr=y
CONFIG_TARGET_armsr_armv8=y
CONFIG_TARGET_armsr_armv8_DEVICE_generic=y
CONFIG_TARGET_PROFILE="DEVICE_generic"

# rootfs tarball —— 这是本项目的核心产物
# 保留 armsr 默认的 ext4/squashfs 镜像一起构建：不显式关闭，
# 避免与 image.mk 的 FILESYSTEMS/TARGET_FILESYSTEMS 过滤逻辑产生边界情况。
# 最终我们只取 rootfs.tar.gz，磁盘镜像可忽略。
CONFIG_TARGET_ROOTFS_TARGZ=y
CONFIG_TARGET_IMAGES_GZIP=y

# ------------------------------------------------------- base system
CONFIG_PACKAGE_luci=y
CONFIG_PACKAGE_luci-ssl=y
CONFIG_PACKAGE_luci-compat=y
CONFIG_PACKAGE_luci-theme-argon=y
CONFIG_PACKAGE_luci-app-argon-config=y
CONFIG_LUCI_LANG_zh_Hans=y

# ---------------------------------------------------------- OpenClash
# 依赖链: luci-compat + dnsmasq-full + bash + curl + ca-bundle + ip-full
#         + ruby + ruby-yaml + kmod-inet-diag + kmod-nft-tproxy + kmod-tun + unzip
CONFIG_PACKAGE_luci-app-openclash=y
CONFIG_PACKAGE_ruby=y
CONFIG_PACKAGE_ruby-yaml=y
CONFIG_PACKAGE_kmod-inet-diag=y
CONFIG_PACKAGE_kmod-nft-tproxy=y
CONFIG_PACKAGE_kmod-tun=y
CONFIG_PACKAGE_unzip=y

# ------------------------------------------------------ web / terminal
CONFIG_PACKAGE_luci-app-ttyd=y
CONFIG_PACKAGE_ttyd=y

# ------------------------------------------------------------- network
CONFIG_PACKAGE_dnsmasq-full=y
CONFIG_PACKAGE_firewall4=y
CONFIG_PACKAGE_nftables=y
CONFIG_PACKAGE_nftables-json=y
CONFIG_PACKAGE_iptables-nft=y
CONFIG_PACKAGE_ipset=y
CONFIG_PACKAGE_kmod-nft-socket=y
CONFIG_PACKAGE_kmod-nft-queue=y
CONFIG_PACKAGE_kmod-nf-conntrack=y
CONFIG_PACKAGE_kmod-nf-nathelper=y
CONFIG_PACKAGE_kmod-ipt-nat=y
CONFIG_PACKAGE_kmod-br-netfilter=y
CONFIG_PACKAGE_kmod-veth=y
CONFIG_PACKAGE_kmod-fs-ext4=y
CONFIG_PACKAGE_kmod-usb-storage=y
CONFIG_PACKAGE_ip-full=y
CONFIG_PACKAGE_miniupnpd=y
CONFIG_PACKAGE_luci-app-upnp=y
CONFIG_PACKAGE_ddns-scripts=y
CONFIG_PACKAGE_luci-app-ddns=y
CONFIG_PACKAGE_luci-app-wol=y

# ------------------------------------------------------- docker stack
# 容器里再跑 docker（DooD）用得上；纯跑 rootfs 时可不管
CONFIG_PACKAGE_docker=y
CONFIG_PACKAGE_dockerd=y
CONFIG_PACKAGE_docker-compose=y
CONFIG_PACKAGE_luci-app-dockerman=y

# -------------------------------------------------------- tooling
CONFIG_PACKAGE_bash=y
CONFIG_PACKAGE_curl=y
CONFIG_PACKAGE_wget-ssl=y
CONFIG_PACKAGE_ca-bundle=y
CONFIG_PACKAGE_ca-certificates=y
CONFIG_PACKAGE_openssl-util=y
CONFIG_PACKAGE_jq=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_nmap=y
CONFIG_PACKAGE_tcpdump=y
CONFIG_PACKAGE_ethtool=y
CONFIG_PACKAGE_coreutils-stat=y
CONFIG_PACKAGE_qrencode=y
CONFIG_PACKAGE_parted=y
CONFIG_PACKAGE_lsblk=y
CONFIG_PACKAGE_block-mount=y
CONFIG_PACKAGE_terminfo=y
CONFIG_PACKAGE_zoneinfo-asia=y
CONFIG_PACKAGE_openssh-sftp-server=y
CONFIG_PACKAGE_dropbear=y

# -------------------------------------------------- excluded (不需要)
# WiFi 驱动 —— 容器共用宿主内核，rootfs 内的 WiFi kmod 无意义
# CONFIG_PACKAGE_kmod-mt7915e is not set
# CONFIG_PACKAGE_kmod-mt7916-firmware is not set
# CONFIG_PACKAGE_wpad-openssl is not set
# LEDE 专有包，IMM 无此包（写了会构建失败）
# CONFIG_PACKAGE_luci-app-turboacc is not set
# CONFIG_PACKAGE_luci-app-partexp is not set
